const Loading = () => {
    console.log("Hi im loading page")
  return (
    <div className="h-screen w-screen flex justify-center items-center z-2948823">
      <div className="text-base md:text-2xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-indigo-500 via-sky-500 to-emerald-500">
        {" { " + "dev.sandesh.com" + " } "}
      </div>
    </div>
  );
};

export default Loading;
